**list** - changeable, indexed, allow duplicates

insert(index, value)
append(value)
extend(iterable)

remove(value)
pop(index)
del
clear()

joining lists
+
extend

**tuple** - unchangeable, indexed, allow duplicates

**set** - unchangeable (you can add and remove), unindexed, does not allow duplicates
add(value)
update(iterable)

remove(value)
discard(value) no error
pop()

clear()

uninion - combine all
update

intersection - elements which both of them have
intersection_update

symeteric_difference - elements which both of them don't have

**dictionary** - does not allow duplicates, key:value
keys()
values()
items()
get()
update(dict)
pop(value)
popitem() - remove last added

**lambda** 

x = lambda a, b: a*b

