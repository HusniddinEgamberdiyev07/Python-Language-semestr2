# Sql statements are commands to give order to database.
# We store data in tables which contains rows and columns.
# We have data types like Number...

# Database -> table -> data

# DDL -> data definition language command
# DML -> data manipulition language
# DCL -> data control language

# CREATE, ALTER, DROP (DDL)
# INSERT, SELECT, DELETE, UPDATE (DML)
# SAVE (DCL)


# Collection of attribute values is row

# CREATE DATABASE siut;
# USE siut;
# DROP DATABASE myFirstDb;
# CREATE TABLE students(sid INT(3), sname VARCHAR(25), grade INT(3));
# INSERT INTO students VALUES(1, "Husniddin", 99);
# INSERT INTO students VALUES(2, "Sardor", 99);
# INSERT INTO students VALUES(3, "Husan", 98);
# SELECT sname, grade FROM students WHERE sname = "Husniddin";

# tkinter helps us to create gui. Kivy. Python qt. wxPython are other tools to create gui.
# tkinter for desktop appliction
# gui -> graphical user interface. tkinter built in python module